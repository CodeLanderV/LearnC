#include <stdio.h>

// insertion sort

void* input_Arr(){
    int a[10], i, n;
    printf("Enter the number of elements you want in the array \n");
    scanf("%d",&n);
    for(i=0; i<n; i++){
        printf("Enter the elements: \n");
        scanf("%d",&a[i]);
    }
    return a[10]
}

void* output_Arr(int* a[10]){
    for(int i=0; i<n; i++){
    printf("elements %d", a[i])
}
}

int main(){
    int a[10];
    input_Arr();
    output_Arr();

    }
